# Melky-Movies

# Usagem

- Clone o projeto:

```shell
git clone https://github.com/rafaScalet/Melky-Movies.git
```

Suba a VM com o vagrant:

```shell
vagrant up
```

Entre na VM usando SSH:

```shell
vagrant ssh
```

Mude para o diretório do projeto:

```shell
cd Melky-Movies
```

Rode o projeto usando o docker:

```shell
docker compose up
```

Abra no seu navegador a URL: `localhost:8080`
